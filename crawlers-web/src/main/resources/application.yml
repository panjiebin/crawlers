server:
  port: 80

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.30.120:3306/crawlers?useSSL=false&useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2B8
    username: root
    password: root

http-client-pool:
  max-total: 20
  default-max-per-route: 4
  connect-timeout: 20000
  connection-request-timeout: 10000
  socket-timeout: 60000
  read-timeout: 60000